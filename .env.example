# Docker Compose Configuration
# Ports in range 7385-7395 to avoid conflicts

# Frontend Configuration
FRONTEND_PORT=7385

# PostgreSQL Configuration
POSTGRES_PORT=7386
POSTGRES_DB=redi_assessment
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here

# Kong API Gateway
KONG_HTTP_PORT=7387
KONG_HTTPS_PORT=7388

# Supabase Studio (Database Management UI)
STUDIO_PORT=7389

# JWT Configuration
# In production, use a strong secret (32+ characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-here

# API Keys (for development only - replace in production with real JWT tokens)
# These should be JWT tokens signed with JWT_SECRET
# For development, you can use the demo tokens below:
ANON_KEY=your-anon-key-here
SERVICE_ROLE_KEY=your-service-role-key-here

# Supabase Configuration for Frontend
# URL points to Kong gateway which routes to appropriate services
VITE_SUPABASE_URL=http://localhost:7387
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: SharePoint Integration (Phase 5)
# VITE_AZURE_CLIENT_ID=your_azure_client_id
# VITE_AZURE_TENANT_ID=your_azure_tenant_id
# VITE_SHAREPOINT_SITE_ID=your_sharepoint_site_id
# VITE_SHAREPOINT_LIST_ID=your_sharepoint_list_id
